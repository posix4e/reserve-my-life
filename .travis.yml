sudo: required

services:
      - docker
language: node_js
node_js:
      - 6
cache:
      directories:
              - node_modules
script:
    - (cd frontend ; npm install && npm test)
    - (cd frontend ; npm run build)
    - ./bin/test
